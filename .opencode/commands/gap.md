---
description: Record a learning gap in the active Professor lesson
---

Call `lesson_state` with `action: "gap"` and record the following text exactly in the `text` argument. Confirm the returned gap id. If the argument is empty, call `lesson_state` with `action: "status"` and list only open gaps.

Gap: $ARGUMENTS
