---
description: Link or inspect the Professor lesson transcript
---

<!-- Managed by opencode-professor. -->

If a path is supplied, call `lesson_log` with that path. If no path is supplied, call `lesson_state` with `action: "status"` and report its configured log path.

Path: $ARGUMENTS
