---
description: Resume a saved Professor lesson
---

Load the `teach` skill and follow its **Resuming a lesson** section. Call `lesson_state` with `action: "list"`; choose the active lesson matching the argument, or the most recently updated active lesson when no argument is given. If none exists, offer to start a new lesson.

Requested topic: $ARGUMENTS
